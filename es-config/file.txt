the word "Death" or "death" is in the text_entry field
A filter reduces the data set being search to the play "Hamlet"
The results are first sorted by their relevancy score in descending 
order and then by the line_id in ascending order
The search supports pagination and returns the first 5 documents
The search results highlight the matched term with <strong>search_term</strong>
Once the team has learned this search, they will be able to abstract out the various 
components and work it into their application to support user customization. 
The team has requested that you save the resulting output of the search to the file 
/home/elastic/results.json on the master node.